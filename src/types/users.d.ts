type User = {
    "id": number,
    "first_name": string,
    "last_name": string,
    "email": string,
    "gender": string,
    "photo"?: string,
};

type location = {
    city: string,
    zip?: number
}
